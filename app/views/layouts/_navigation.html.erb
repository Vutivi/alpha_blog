<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to root_path, class: "navbar-brand", id: "logo" do %>
        <span class="glyphicon glyphicon-sort-by-alphabet"></span><%= "Alpha Blog" %>
      <% end %>
    </div>
    <ul class="nav navbar-nav">
      <li>
          <%= link_to articles_path do %>
            <span class="glyphicon glyphicon-zoom-out"></span><%= " Explore articles" %>
          <% end %>
      </li>
      <li>
          <%= render 'layouts/categories'%>
      </li>
      <li>
          <%= link_to users_path, :class => 'button white' do %>
            <span class="glyphicon glyphicon-plus"></span><%= " Follow users" %>
          <% end %>
      </li>
    </ul>
    <form class="navbar-form navbar-left" action="/action_page.php">
      <div class="input-group">
        <input type="text" class="form-control" placeholder="Search">
        <div class="input-group-btn">
          <button class="btn btn-default" type="submit">
            <i class="glyphicon glyphicon-search"></i>
          </button>
        </div>
      </div>
    </form>
    <ul class="nav navbar-nav navbar-right">
      <% if logged_in? %>

        <li class="dropdown">
          <a class="dropdown-toggle button white'" data-toggle="dropdown" href="#">Settings
          <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View profile", user_path(current_user) %></li>
            <li><%= link_to "edit profile", edit_user_path(current_user) %></li>
          </ul>
        </li>
        <li>
            <%= link_to sign_out_path, method: :delete, :class => 'button white' do %>
              <span class="glyphicon glyphicon-user"></span>Sign out, <%= "#{current_user.admin? ? "admin" : current_user.username}" %>
            <% end %>
        </li>
      <% else %>
        <li>
          <%= link_to sign_in_path, :class => 'button white' do %>
            <span class="glyphicon glyphicon-log-in"></span><%= " Sign in" %>
          <% end %>
        </li>
        <li>
            <%= link_to sign_up_path, :class => 'button white' do %>
              <span class="glyphicon glyphicon-plus"></span><%= " Sign up" %>
            <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
